<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use AppCompat theme to support per-app language preferences.
         This is REQUIRED for AppCompatActivity to function on compileSdk 36. -->
    <style name="Theme.NajiAethers" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/aether_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>